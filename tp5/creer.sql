CREATE TABLE AUTEUR (ID INTEGER NOT NULL, EMAIL VARCHAR(255), NOM VARCHAR(20) NOT NULL, PRENOM VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE LIVRE (ID INTEGER NOT NULL, CATEGORIE VARCHAR(255), TITRE VARCHAR(255), AUTEUR_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE LIVRE ADD CONSTRAINT FK_LIVRE_AUTEUR_ID FOREIGN KEY (AUTEUR_ID) REFERENCES AUTEUR (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
